# SPDX-License-Identifier: GPL-2.0+

# Brightspeed XG2010G U-Boot Bootloader

This repository contains a U-Boot bootloader build for the Brightspeed
XG2010G. It is based on the current upstream mainline U-Boot tree used by this
repository, with XG2010G board support added for the Airoha AN7581-class
platform.

Upstream U-Boot remains GPL-2.0+ licensed. The original upstream notice is:

> (C) Copyright 2000 - 2013 Wolfgang Denk, DENX Software Engineering,
> wd@denx.de.

See `COPYING` and `Licenses/README` for the complete U-Boot licensing
information. This project keeps that GPL basis and adds XG2010G board files,
documentation, and GitHub Actions packaging.

## 项目用途

本仓库用于 Brightspeed XG2010G 的 U-Boot 引导程序，目标是替换原厂
`/dev/mtd0` 分区中的 `bootloader`。XG2010G 的 Airoha 平台启动镜像通常不是
单独的裸 `u-boot.bin`，而是一个从 BL2 开始签名的完整启动包/FIP，里面包含
BL2、BL31、U-Boot/BL33 和证书材料。

本项目的边界固定为：

- `mtd0 bootloader` 仍只使用 `0x00000000-0x00200000`，大小 2 MiB。
- `uenv` `0x00200000-0x00400000` 不覆盖，用于 U-Boot 环境。
- `dsd` `0x00400000-0x00600000` 不覆盖，用于保留原厂数据。
- `0x00600000-0x1be00000` 作为后续系统使用的 UBI 分区范围。
- `0x1be00000-0x20000000` 约 66 MiB 保留给 NAND 坏块替代/BMT。
- GitHub Actions 只输出已签名 bootloader 产物，不提供未签名刷写选项。

## 安全提醒

刷写 bootloader 有变砖风险。开始前必须保存完整原厂备份，并确认 NAND 型号、
容量、页大小、擦除块大小、ECC/OOB 方式、BootROM 镜像格式和 Trusted Boot
策略与本项目匹配。

特别注意：

- `mtd0` 的正确长度是 `0x200000`，即 2 MiB。
- 不要使用 `0x2000000` 作为 bootloader 擦写长度；那是 32 MiB，会越过
  `mtd0`、`uenv`、`dsd` 并进入系统区域。
- 不要把系统镜像、UBI 镜像或普通 `image.ub` 当作 bootloader 写入 `mtd0`。
- `saveenv` 会写 `uenv` 区域；改环境前建议先备份当前环境。
- 如果 Trusted Boot 私钥曾出现在聊天、日志、工单或公开位置，应视为已泄露，
  正式发布前建议轮换密钥。

## 启动链

Airoha AN7581/AN7583 类平台通常按以下顺序启动：

1. BootROM 从 NAND 启动位置读取镜像。
2. BootROM 加载 BL2。BL2 负责早期硬件初始化，尤其是 DDR 初始化，并验证后续镜像。
3. BL2 加载 BL31。BL31 是 ARM Trusted Firmware 的 EL3 运行时固件。
4. BL31 加载 BL33。这里的 BL33 是本仓库编译出的 U-Boot。
5. U-Boot 加载 Linux/FIT/UBI 系统镜像。

因此，普通构建得到的 `u-boot.bin` 只是 BL33 候选文件，不能直接替换完整
`mtd0`。

## BL2、BL31 与签名

BL2 和 BL31 不是 U-Boot 生成的文件，也不能由 Trusted Boot Key 生成。它们
必须来自与设备硬件匹配的 Airoha 平台固件输入，或者来自完整可用的 Airoha
TF-A/DDR 初始化源码和签名配置。

当前兼容方案是：

1. 使用从原厂 `mtd0` 备份提取的 BL2/preloader、BL31 和 `mtd0` 前导区。
2. 编译本仓库得到新的 `u-boot.bin`，作为 BL33。
3. 使用 TF-A 的 `fiptool` 和 `cert_create` 重新组装 signed FIP。
4. 签名链从 BL2 开始：`--tb-fw` BL2、`--soc-fw` BL31、`--nt-fw` U-Boot/BL33。
5. 将 signed FIP 放回 2 MiB `mtd0` 镜像的 `0x800` 偏移。

仓库内置的最小启动输入来自
`original-device-backup-20260816/mtd0-bootloader-stock.bin`：

| 文件 | 说明 | SHA256 |
| --- | --- | --- |
| `board/airoha/xg2010g/firmware/mtd0-prefix.bin` | `mtd0` 中 FIP 前的 `0x800` 字节前导区 | `82830140f4f8842702d0569065c27071b7cc24e0876e6c487cb4d9d81c294dd7` |
| `board/airoha/xg2010g/firmware/ubi-preloader.bin` | BL2/preloader 裸文件 | `6dfd08d05691cf2d89e0892b4e597a8d9a8ebab80a362c9b7a02868ffcddd1c8` |
| `board/airoha/xg2010g/firmware/bl31.bin` | BL31/EL3 runtime firmware | `e79bb6960e71384a4f8b67be0d0cd0f64042b174b4e6b2502428238f548245f3` |

这些文件只适用于匹配的 XG2010G 硬件、NAND、DDR 和安全启动策略。若板级修订
不同，应从对应设备备份重新提取并覆盖 workflow 输入。

## 证书产物说明

签名过程中会临时生成：

- `trusted_key.crt`
- `tb_fw.crt`
- `tb_fw_key.crt`
- `soc_fw.crt`
- `soc_fw_key.crt`
- `nt_fw.crt`
- `nt_fw_key.crt`

这些是 TF-A `cert_create` 生成的 X.509 证书链/内容证书，用来把 BL2、BL31、
U-Boot/BL33 的哈希、公钥关系和 NV counter 等元数据放进 signed FIP。它们不
是私钥，也不应包含 `XG2010G_TB_PRIVATE_KEY` 的私钥内容。

不过这些单独的 `.crt` 文件对最终刷机不是必需文件；它们会暴露证书链结构、
公钥材料、镜像哈希和构建元数据。signed FIP/mtd0 内部仍会包含启动验证必需
的证书内容，这是 Trusted Boot 启动链的一部分。为减少公开产物里的敏感上下文，
本仓库 workflow 只在签名过程中临时生成单独 `.crt` 文件，不上传到 workflow
artifact，也不上传到 GitHub Releases。最终需要发布的是 signed mtd0/FIP、
XMODEM 救砖裸文件、SHA256 和构建信息。

## 512 MiB SLC NAND 分区

原厂可见 MTD 分区如下：

| 地址范围 | 分区名 |
| --- | --- |
| `0x00000000-0x00200000` | `bootloader` |
| `0x00200000-0x00400000` | `uenv` |
| `0x00400000-0x00600000` | `dsd` |
| `0x00600000-0x00964842` | `kernel` |
| `0x00964940-0x025d4940` | `rootfs` |
| `0x00600000-0x04600000` | `tclinux` |
| `0x04600000-0x04964842` | `kernel_slave` |
| `0x04964940-0x065d4940` | `rootfs_slave` |
| `0x04600000-0x08600000` | `tclinux_slave` |
| `0x08600000-0x1be00000` | `system` |

本项目规划的目标边界如下：

| 区域 | 起始 | 结束（不含） | 大小 | 用途 |
| --- | --- | --- | --- | --- |
| `bootloader` | `0x00000000` | `0x00200000` | 2 MiB | 完整 signed mtd0/FIP，只在确认后替换 |
| `uenv` | `0x00200000` | `0x00400000` | 2 MiB | U-Boot 环境，默认保留 |
| `dsd` | `0x00400000` | `0x00600000` | 2 MiB | 原厂数据，必须保留 |
| `ubi` | `0x00600000` | `0x1be00000` | 440 MiB | 后续系统镜像/UBI 数据 |
| `reserved_bmt` | `0x1be00000` | `0x20000000` | 66 MiB | NAND 坏块替代/BMT 预留 |

后续系统升级只应刷写 `ubi` 区域，也就是偏移 `0x00600000`、长度
`0x1b800000`。不要覆盖 `bootloader`、`uenv`、`dsd` 或尾部 BMT 预留区。

## GitHub Actions 与 Releases

`.github/workflows/build-mtd0.yml` 会在 GitHub Actions 上构建并强制签名。
仓库 Secret `XG2010G_TB_PRIVATE_KEY` 必须存在；如果签名失败，workflow 失败，
不会上传未签名 bootloader artifact。

Release 或 workflow artifact 中常见文件如下：

| 文件 | 用途 |
| --- | --- |
| `xg2010g-...-mtd0-signed.bin` | 完整 2 MiB `/dev/mtd0` bootloader 镜像，用于替换 `bootloader` 分区 |
| `xg2010g-...-fip-signed.bin` | signed FIP 本体，位于完整 mtd0 镜像的 `0x800` 偏移 |
| `xg2010g-...-ubi-preloader.bin` | BL2/preloader 裸文件，用于 X 模式第一段 XMODEM |
| `xg2010g-...-ubi-bl31-uboot.fip` | BL31 + U-Boot/BL33 FIP，用于 X 模式第二段 XMODEM 和 Web Recovery |
| `xg2010g-...-bl31.bin` | BL31 裸文件，便于核对和离线调试 |
| `xg2010g-...-u-boot-raw.bin` | 本次编译出的裸 U-Boot/BL33，不可直接写入 `mtd0` |
| `ubi-preloader.bin` | 不带版本号的 BL2/preloader，救砖时方便选择 |
| `ubi-bl31-uboot.fip` | 不带版本号的 BL31 + U-Boot FIP，救砖时方便选择 |
| `bl31.bin` | 不带版本号的 BL31 |
| `u-boot-raw.bin` | 不带版本号的裸 U-Boot/BL33 |
| `sha256sums.txt` | 所有产物 SHA256 |
| `build-info.txt` | 构建 commit、日期、签名状态和镜像边界 |

使用 Release 文件时先校验 SHA256。Linux/WSL 可用：

```console
sha256sum -c sha256sums.txt
```

Windows 可用：

```console
CertUtil -hashfile xg2010g-...-mtd0-signed.bin SHA256
```

只有 `xg2010g-...-mtd0-signed.bin` 是完整 2 MiB `mtd0` 镜像。其它裸文件或
FIP 文件用于救砖、调试或 Web Recovery，不要当作完整 `mtd0` 直接写入
`0x00000000`。

## TTL 中断后通过 TFTP 刷入 mtd0

该流程适用于已经能进原厂 U-Boot/TTL 命令行，并且当前 bootloader 提供
Airoha `flash` 命令的情况。

准备：

1. TTL 接好，启动时中断进入 U-Boot。
2. 电脑网线连接设备 1G 网口。
3. 电脑 IP 设置为 `192.168.0.205/24`。
4. U-Boot 侧设备 IP 使用 `192.168.0.1`。
5. 待刷文件使用 Release 中的 `xg2010g-...-mtd0-signed.bin`，文件大小必须是
   `2097152` 字节。

推荐方式是电脑运行 TFTP server，U-Boot 主动拉取文件：

```console
setenv ipaddr 192.168.0.1
setenv serverip 192.168.0.205
setenv loadaddr 0x81800000
tftpboot ${loadaddr} xg2010g-...-mtd0-signed.bin
echo ${filesize}
crc32 ${loadaddr} ${filesize}
```

确认 `filesize` 为 `200000` 或 `0x200000` 后，只擦写 mtd0 的 2 MiB：

```console
flash erase 0x000000 0x200000
flash write 0x000000 0x200000 0x81800000
reset
```

这里的参数顺序按原厂/Airoha `flash` 帮助信息复核：

- `flash erase [addr] [len]`
- `flash write [dst] [len] *[src]`

所以 mtd0 写入命令是：目的地址 `0x000000`，长度 `0x200000`，来源内存地址
`0x81800000`。

如果当前 bootloader 不是 `tftpboot` 拉取模式，而是提示电脑执行 Windows
TFTP 上传，例如：

```console
tftp -i 192.168.0.1 put image.ub
```

那么必须确认设备端确实处在 TFTP receive/server 模式，并且 `image.ub` 只是
传输时的文件名。此时也应发送同一个 signed mtd0 产物，不能把系统镜像
`image.ub` 当 bootloader 写入 `mtd0`。

## X 模式救砖与 HTTP Web Recovery

如果 `mtd0` 写坏导致 NAND 无法启动，Airoha BootROM 通常仍可进入串口
X 模式加载临时引导。该流程需要 TTL 串口和支持 XMODEM 的终端工具。

首次使用救砖链时，可能需要先加载一次 `bootext.ram`：

1. 断电。
2. 按住 `RESET` 键，同时通电启动。
3. 终端显示 `CCCC` 时表示设备已进入 X 模式。
4. 打开终端菜单：文件 -> 传输 -> XMODEM -> 发送。
5. 选择 `bootext.ram`，等待传输完成。之后通常不再需要重复加载该文件。

正式救砖/刷机步骤：

1. 断电，按住 `RESET` 键，同时通电启动。
2. 按 `X`/`x`，终端显示 `CCCC` 后进入 XMODEM 接收。
3. 打开：文件 -> 传输 -> XMODEM -> 发送。
4. 发送 Release 里的 `xg2010g-...-ubi-preloader.bin`，或不带版本号的
   `ubi-preloader.bin`。
5. 传输完成后设备会自动重启。
6. 再次断电，按住 `RESET` 键，同时通电启动。
7. 提示 `Press x to load BL31 + U-Boot FIP` 时输入 `x`。
8. 再次打开：文件 -> 传输 -> XMODEM -> 发送。
9. 发送 `xg2010g-...-ubi-bl31-uboot.fip`，或不带版本号的
   `ubi-bl31-uboot.fip`。
10. 第二段 XMODEM 进度到 100% 前提前按住 `RESET`，等待设备灯完成闪烁并进入
    流水式闪烁后再松开。
11. 浏览器使用无痕模式访问 `http://192.168.1.1/uboot.html`。
12. 选择系统镜像 `ubi-squashfs-sysupgrade.itb`。
13. `BL2` 选择 `xg2010g-...-ubi-preloader.bin` 或 `ubi-preloader.bin`。
14. `U-Boot` 选择 `xg2010g-...-ubi-bl31-uboot.fip` 或 `ubi-bl31-uboot.fip`。
15. 必须勾选“先重建 UBI”。
16. 等待数分钟完成刷写。
17. 刷写完成后务必断电重启设备。

`bootext.ram` 属于平台救援链文件，不由本仓库的 U-Boot 编译生成；需要保留
已验证可用的原厂/平台版本。

## 正常引导系统

Web Recovery 刷写 `ubi-squashfs-sysupgrade.itb` 并勾选“先重建 UBI”后，正常
启动应直接断电重启，不再按 `RESET`，让 U-Boot 按默认环境启动系统。

如果需要恢复原厂/legacy `tclinux` 引导环境，可在 TTL 中断后参考以下命令。
这些命令会写入 `uenv`，请先确认当前环境已备份：

```console
setenv bootflag 0
setenv one flash read 0x602100 0x4000000 $loadaddr
setenv two "; bootm"
setenv bootcmd "$one$two"
setenv one
setenv two
setenv bootargs 'sdram_conf=0x00108893 vendor_name=ECONET Technologies Corp. product_name=xPON ONU ubi.mtd=system ethaddr=00:AA:BB:01:23:40 snmp_sysobjid=1.2.3.4.5 country_code=ff ether_gpio=0c power_gpio=1515 dsl_gpio=0b internet_gpio=02 multi_upgrade_gpio=0b020400000000000000000000000000 onu_type=61 qdma_init=69bb root=/dev/mtdblock4 ro console=ttyS0,115200n8 earlycon bootflag=0 serdes_sel=0 serdes_pon=000 serdes_ethernet=411 serdes_wifi1=005 serdes_wifi2=413 serdes_usb1=111 serdes_usb2=000 tclinux_info=0x1fc4d8b,0x5724,0x36101e,0x366840,0x3000000,0x0,0x2000,0x0,0x2000,0x0'
setenv serdes_ethernet 411
saveenv
reset
```

`serdes_ethernet` 说明：

- `411`：光口/PON 方向，上网 2.5G 正常。
- `421`：网口方向；已知场景下 2.5G 网口不能用于上网，不建议作为默认值。

如果系统已经启动到 OpenWrt/failsafe，需要清理 overlay 或重新设置密码，可用：

```console
rm -rf /overlay
mount_root
passwd
reboot
```

## 本地构建

推荐在 WSL/Linux 文件系统中构建，或用 `git archive` 导出临时构建副本，避免
Windows drvfs 上脚本可执行位和符号链接问题：

```console
make CROSS_COMPILE=aarch64-linux-gnu- xg2010g_defconfig
make CROSS_COMPILE=aarch64-linux-gnu- -j$(nproc)
```

本地构建出的 `u-boot.bin` 只是 BL33 候选文件。正式可刷写 `mtd0` 镜像请使用
GitHub Actions/Release 生成的 signed artifact。

更多板级细节见 `doc/board/airoha/xg2010g.rst`。
