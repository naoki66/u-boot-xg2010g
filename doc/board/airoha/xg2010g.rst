XG2010G U-Boot
==============

本文档记录 XG2010G 的 U-Boot 板级配置、启动链和 NAND 布局。
XG2010G 使用 Airoha AN7581 类 SoC。当前配置基于上游 U-Boot 已有的
AN7581 支持，并在本项目中加入 XG2010G 的板级设备树。

安全提醒
--------

刷写 bootloader 或 NAND 前必须准备完整的原厂备份，并确认 NAND 型号、
容量、页大小、擦除块大小、ECC/OOB 方式和 BootROM 的镜像格式完全匹配。
错误的 mtd0 镜像可能导致设备无法从 NAND 启动；本项目不提供盲刷命令。

除 ``board/airoha/xg2010g/firmware`` 下明确记录 SHA256 的最小启动输入外，
本仓库不得提交以下内容：

* 完整原厂 mtd0、完整固件备份或未筛选的原厂二进制；
* Trusted Boot 私钥、密钥目录、签名配置和未公开的证书材料；
* 仅适用于单台设备的序列号、MAC 地址或工厂校准数据；
* 构建目录和未经验证的最终刷写镜像。

启动链
------

AN7581 类平台的典型启动顺序如下：

1. 片内 BootROM 从 NAND 的启动位置读取启动镜像。
2. BootROM 启动 BL2。BL2 负责早期硬件初始化，尤其是 DDR 初始化，
   并根据平台策略验证和装载后续组件。
3. BL2 装载 BL31。BL31 是 ARM Trusted Firmware 的 EL3 运行时固件，
   负责安全监控器和安全世界/普通世界切换。
4. BL31 将控制权交给 BL33。这里的 BL33 通常就是 U-Boot。
5. U-Boot 访问 NAND 上的 UBI/UBIFS 或 FIT 镜像，最后启动 Linux。

因此，单独编译出来的 ``u-boot.bin`` 通常不能直接替代完整的 mtd0。
在使用 FIP 启动的设备上，mtd0 中应是与该平台 BootROM/BL2 约定的完整
FIP，而不是只有 BL33 的裸 U-Boot。

BL2、BL31 和 Trusted Boot Key
-----------------------------

BL2 和 BL31 不是普通 U-Boot 配置项，也不能由 Trusted Boot Key “生成”。
它们有用，而且是当前可启动方案的关键输入：BL2 负责早期硬件和 DDR
初始化，BL31 负责 EL3 运行时环境。除非拿到 Airoha 匹配平台的
TF-A/DDR 初始化源码和完整签名配置，否则不能新生成等价 BL2/BL31。
本项目的兼容性方案是：

* 从同一台设备的原厂 mtd0/FIP 中提取并保留与硬件匹配的 BL2 和 BL31；
* 编译本项目得到新的 U-Boot，作为 BL33；
* 使用与 Airoha 平台镜像流程兼容的 ``fiptool`` 重新组装 FIP；
* 按 BootROM/BL2 的实际校验策略重新生成或更新证书和签名。

原厂 FIP 的证书链不能因为只替换了 BL33 就默认继续有效。若证书中包含
BL33 的摘要，替换 BL33 后必须重新生成对应的内容证书和签名；若设备启用
了锁定的 Trusted Boot，还必须使用设备认可的签名根。没有正确的签名密钥
或密钥授权时，新 FIP 可能在 BootROM 或 BL2 阶段被拒绝。

已从原厂 mtd0 备份确认其包含 FIP 头 ``0xaa640001``，且 FIP 位于 mtd0
偏移 ``0x800``。备份中可见 BL2、BL31、BL33/U-Boot 和 Trusted Boot
证书。下面这些偏移仅用于分析原厂文件，不能当作所有 XG2010G 设备的
通用格式：

* BL2：FIP 相对偏移约 ``0x400``；
* BL31：FIP 相对偏移约 ``0x1f800``；
* BL33：FIP 相对偏移约 ``0x29c00``。

重新组装 FIP 时应以目标平台提供的 ``fiptool``、FIP UUID 定义、载入地址、
对齐要求和签名配置为准。不要把本仓库的 U-Boot 配置误认为已经完成了
Airoha 私有 FIP 签名流程。

当前仓库内置了从 ``original-device-backup-20260816/mtd0-bootloader-stock.bin``
提取的最小启动输入：

.. list-table::
   :header-rows: 1
   :widths: 40 25 35

   * - 文件
     - 说明
     - SHA256
   * - ``board/airoha/xg2010g/firmware/mtd0-prefix.bin``
     - mtd0 中 FIP 前的 ``0x800`` 字节前导区
     - ``82830140f4f8842702d0569065c27071b7cc24e0876e6c487cb4d9d81c294dd7``
   * - ``board/airoha/xg2010g/firmware/ubi-preloader.bin``
     - BL2/preloader
     - ``6dfd08d05691cf2d89e0892b4e597a8d9a8ebab80a362c9b7a02868ffcddd1c8``
   * - ``board/airoha/xg2010g/firmware/bl31.bin``
     - BL31/EL3 runtime firmware
     - ``e79bb6960e71384a4f8b67be0d0cd0f64042b174b4e6b2502428238f548245f3``

这些内置文件只适用于匹配的 XG2010G 硬件、NAND 和 DDR 配置。若板级修订、
NAND 几何、DDR 参数或安全启动策略不同，应从对应设备的完整备份重新提取
BL2/BL31/prefix，并通过 Secret/私有 URL 覆盖 workflow 输入。

NAND 分区
---------

按当前目标设备的 512 MiB NAND 规划，分区如下。地址和大小均为字节：

.. list-table::
   :header-rows: 1
   :widths: 25 20 20 35

   * - 分区
     - 起始
     - 结束（不含）
     - 用途
   * - ``bootloader``
     - ``0x00000000``
     - ``0x00200000``
     - 2 MiB，完整启动镜像/FIP，只读保护
   * - ``uenv``
     - ``0x00200000``
     - ``0x00400000``
     - 2 MiB，U-Boot 环境所在区域
   * - ``dsd``
     - ``0x00400000``
     - ``0x00600000``
     - 2 MiB，保留原有数据
   * - ``ubi``
     - ``0x00600000``
     - ``0x1be00000``
     - 440 MiB，项目唯一系统 UBI，必须包含 ``fit`` 卷
   * - ``reserved_bmt``
     - ``0x1be00000``
     - ``0x20000000``
     - 66 MiB，坏块替代和 BMT 预留，只读保护

这里的 ``ubi`` 是给 UBI 使用的 MTD 分区；“UBI 2.0”表示目标系统的
UBI 分区/布局约定，不是 U-Boot 中需要选择的另一个 UBI 工具版本。
UBI 会根据 NAND 的实际擦除块和坏块情况管理可用 PEB。尾部预留区不能
被系统镜像、UBI 镜像或升级脚本覆盖。原厂 TTL 中的 ``tclinux``、
``tclinux_slave``、``system`` 仅用于校核，不属于本项目兼容布局。TTL
确认本板使用 512 MiB W25N04K，擦除块 128 KiB、页 2 KiB、OOB 128 字节；
项目 U-Boot/Recovery 会拒绝不符合该几何和 440 MiB 分区大小的 ``ubi``。
板级初始化会自动清理原厂 ``ubi.mtd=system/tclinux*`` 和
``root=/dev/mtdblock*`` 参数，避免 Linux 误挂载原厂分区。

bootargs 参数说明
------------------

本节根据 XG2010G 原厂 TTL、Airoha/ECONET SDK 启动参数解析代码和实机 PHY
初始化日志校核。文档或聊天中出现的 ``\_`` 只是 Markdown 转义，实际参数名中
只有普通下划线，不能把反斜杠传给内核。

``onu_type``、``qdma_init``、``bootflag`` 和 ``serdes_*`` 由 SDK 使用
``%x``、``%hx`` 或 ``%hhx`` 解析，因此没有 ``0x`` 前缀时仍按十六进制解释。
例如 ``onu_type=71`` 实际是 ``0x71``，``qdma_init=69bb`` 实际是
``0x69bb``。

基础与平台参数
~~~~~~~~~~~~~~

.. list-table::
   :header-rows: 1
   :widths: 24 20 56

   * - 参数
     - 当前值
     - 含义
   * - ``sdram_conf``
     - ``0x00108893``
     - Airoha/ECONET SDRAM 配置标识；DDR 早期初始化由匹配硬件的 BL2 完成，当前 SDK 未公开完整位定义，应保留原厂值。
   * - ``vendor_name``
     - ``ECONET Technologies Corp.``
     - 厂商标识，供厂商系统接口或用户态组件识别。
   * - ``product_name``
     - ``xPON ONU``
     - 产品类别名称。
   * - ``snmp_sysobjid``
     - ``1.2.3.4.5``
     - SNMP ``sysObjectID``；当前为原厂通用占位 OID，不影响启动。
   * - ``country_code``
     - ``ff``
     - 一字节区域字段；``0xff`` 更接近默认/未指定值，不是 ISO 国家代码。
   * - ``console``
     - ``ttyS0,115200n8``
     - Linux 主控制台：``ttyS0``、115200 波特率、8N1。
   * - ``earlycon``
     - （无值）
     - 在正式串口驱动初始化前启用早期内核日志。
   * - ``bootflag``
     - ``0``
     - 原厂双镜像中表示 Main Image；项目不再使用原厂双分区回退，仅保留兼容字段，实际失败回退由 ``run boot_ubi || http_recovery`` 控制。

``vendor_name`` 和 ``product_name`` 含空格，这是原厂命令行的既有写法。标准
Linux 命令行解析可能把它们截断为 ``vendor_name=ECONET`` 和
``product_name=xPON``；原厂实机仍可正常启动。若用户态必须得到完整值，应使用
带内嵌双引号的值或不含空格的标识。

UBI 与根文件系统
~~~~~~~~~~~~~~~~

.. list-table::
   :header-rows: 1
   :widths: 26 74

   * - 参数
     - 含义
   * - ``ubi.mtd=ubi``
     - 将项目的 ``ubi`` MTD 分区挂接为 ``ubi0``；这是新布局的正确值，不能改回原厂 ``ubi.mtd=system``。
   * - ``ubi.block=0,fit``
     - 从 ``ubi0`` 的 ``fit`` 卷建立块设备，供 FIT 根文件系统路径使用。
   * - ``root=/dev/fit0``
     - 使用 FIT 导出的根文件系统作为 Linux 根设备。
   * - ``rootwait``
     - 等待根设备出现后再挂载。
   * - ``ramdisk_size=65536``
     - 保留 64 MiB ramdisk 上限，兼容可能带 initramfs 的 FIT。
   * - ``rdinit=/sbin/init``
     - 使用 initramfs 时指定首个用户态进程。

只有 ``ubi.mtd=ubi`` 还不足以选择根文件系统；项目默认 ``bootargs`` 已包含
``ubi.block``、``root`` 和等待参数。不要使用截断在 ``serdes_usb2=000`` 的
不完整命令行。

GPIO/LED 兼容字段
~~~~~~~~~~~~~~~~~

``ether_gpio=0c``、``dsl_gpio=0b`` 和 ``internet_gpio=02`` 是原厂平台的
单字节指示字段；``power_gpio=1515`` 是厂商打包字段，不能按十进制 GPIO 1515
理解。``multi_upgrade_gpio=0b020400000000000000000000000000`` 是 16 字节升级
状态 GPIO/LED 配置表，不是单一 GPIO 编号。当前公开 SDK 只列出这些环境变量名，
没有给出 ``power_gpio`` 和 ``multi_upgrade_gpio`` 的完整打包格式，未取得厂商
解码实现前不应自行改写。

ONU 类型：``onu_type=71``
~~~~~~~~~~~~~~~~~~~~~~~~

``onu_type`` 是按位打包的十六进制字节，不是单一的模式编号。SDK 使用
``ONUTYPE_MASK=0x03``、``COMBOPON_MASK=0x04``、``BBF247_MASK=0x08`` 和
``ONUMODE_MASK=0xf0`` 解码，组合公式为：

.. code-block:: text

   onu_type = (PON_MODE << 4) | (BBF247_BIT << 3) | (COMBO_BIT << 2) | ONU_TYPE

低位字段定义：

.. list-table::
   :header-rows: 1
   :widths: 18 14 68

   * - 位
     - 值
     - 含义
   * - ``[1:0]``
     - ``0``
     - unknown，未知 ONU 类型
   * - ``[1:0]``
     - ``1``
     - SFU，偏桥接型 ONU
   * - ``[1:0]``
     - ``2``
     - HGU，家庭网关型 ONU
   * - ``2``
     - ``0/1``
     - 非 Combo PON / Combo PON
   * - ``3``
     - ``0/1``
     - 非 BBF.247 / BBF.247

高四位 ``[7:4]`` 是 SDK 的 ``XMCSIF_WanDetectionMode_t``（``ONUMODE_MASK``）枚举：

.. list-table::
   :header-rows: 1
   :widths: 12 28 22 38

   * - 高四位
     - SDK 枚举
     - SDK 打印名
     - 协议/速率含义
   * - ``0x0``
     - ``AUTO``
     - ``auto``
     - 自动检测（兼容旧版 preversion）
   * - ``0x1``
     - ``GPON``
     - ``GPON``
     - GPON
   * - ``0x2``
     - ``EPON``
     - ``EPON``
     - 普通 1G EPON
   * - ``0x3``
     - ``10G_1G_EPON``
     - ``XEPON-ASYM``
     - XE-PON 非对称：下行 10G、上行 1G
   * - ``0x4``
     - ``10G_10G_EPON``
     - ``XEPON-SYM``
     - XE-PON 对称：下行/上行均 10G
   * - ``0x5``
     - ``1G_1G_EPON``
     - ``XEPON_1G``
     - XEPON 1G+1G（10G MAC 的 1G/1G 模式）
   * - ``0x6``
     - ``XGPON``
     - ``XGPON``
     - XG-PON
   * - ``0x7``
     - ``XGSPON``
     - ``XGSPON``
     - XGS-PON（对称 10G）
   * - ``0x8``
     - ``NGPON2_10G_10G``
     - ``NGPON2-10G_10G``
     - NG-PON2：10G/10G
   * - ``0x9``
     - ``NGPON2_10G_2G``
     - ``NGPON2-10G_2G``
     - NG-PON2：10G/2G
   * - ``0xA``
     - ``NGPON2_2G_2G``
     - ``NGPON2-2G_2G``
     - NG-PON2：2G/2G
   * - ``0xB``
     - ``GPON_SYM``
     - ``GPON-SYM``
     - GPON 对称模式
   * - ``0xC``
     - ``TURBO_EPON``
     - ``TURBO-EPON``
     - Turbo EPON

常用编码示例（低位取非 Combo、非 BBF.247）：

.. list-table::
   :header-rows: 1
   :widths: 42 58

   * - 组合
     - ``onu_type``
   * - GPON + SFU
     - ``0x11``（命令行可写 ``11``）
   * - EPON + SFU
     - ``0x21``（命令行可写 ``21``）
   * - XGPON + SFU
     - ``0x61``（命令行可写 ``61``）
   * - XGSPON + SFU
     - ``0x71``（命令行可写 ``71``）
   * - XGSPON + HGU
     - ``0x72``（命令行可写 ``72``）

因此 ``onu_type=71`` 中的 ``71`` 实际是十六进制 ``0x71``：高四位 ``0x7`` 为
XGS-PON，低两位 ``0x1`` 为 SFU，bit 2/3 均为 0，即 **XGS-PON + SFU、非 Combo、
非 BBF.247**。这与原厂 TTL 中的 ``PON MAC GET ONU_TYPE = SFU`` 和
``PON MAC GET ONU_MODE = XGSPON`` 完全一致。

QDMA：``qdma_init=69bb``
~~~~~~~~~~~~~~~~~~~~~~~~

``0x69bb`` 的有效配置为：LAN/WAN payload selector 均为 ``3``（256 字节档），
LAN/WAN DSCP/描述符均走 SRAM，并开启 ``FAST_WAN``、``FAST_XSI_PCIE1``、
``FAST_XSI_ETHER``、``FAST_XSI_PON``。EN7581 SDK 因此预留 LAN 4 MiB、WAN 5 MiB，
合计 9 MiB；原厂 TTL 的 ``0x400000``/``0x500000`` 输出与此一致。

SerDes 参数
~~~~~~~~~~~

EN7581 使用六个独立的 ``serdes_*`` 参数。每个值是 12 位十六进制配置字：

.. code-block:: text

   [11:8] PHY 类型
    [7:4] Ethernet 类型：0=无，1=LAN，2=WAN
    [3:0] 当前 SerDes 通道的接口选择

.. list-table::
   :header-rows: 1
   :widths: 28 38 34

   * - 参数
     - 解码
     - 实机对应
   * - ``serdes_pon=000``
     - 原生 PON 接口，Ethernet/PHY 字段为零
     - XGS-PON
   * - ``serdes_ethernet=411``
     - PHY profile C + LAN + USXGMII
     - RTL8261N，MDIO 地址 5
   * - ``serdes_wifi1=005``
     - WiFi1/PCIe0 选择 ``NONE``
     - 通道关闭
   * - ``serdes_wifi2=413``
     - PHY profile C + LAN + USXGMII
     - PCIe1 通道改作 RTL8261N Ethernet，MDIO 地址 8
   * - ``serdes_usb1=111``
     - AN8811 profile + LAN + HSGMII
     - USB1 通道改作 EN8811H 2.5G，MDIO 地址 ``0xf``
   * - ``serdes_usb2=000``
     - USB3 接口，Ethernet/PHY 字段为零
     - 未改作 Ethernet

``WiFi2``、``USB1`` 是 SoC SerDes 复用通道名称，不表示设备实际装有 Wi-Fi
或使用 USB 功能。``serdes_sel=0`` 在 EN7581 分支主要是兼容残留，不会覆盖六路
独立配置。原厂 ``serdes_ethernet=411`` 不应改成 ``421``，后者会把 Ethernet
SerDes 的角色从 LAN 改成 WAN，和原厂 TTL 及端口映射不符。

校核依据
~~~~~~~~

* 原厂 TTL：内核命令行、QDMA 缓冲区、PHY 初始化和 XGS-PON/SFU 输出。
* SDK ``linux/arch/arm64/mach-econet/ecnt_bootargs.c``：十六进制参数解析。
* SDK ``private/xpon_10g/src/ic/AN7581.c``、``union_ic_init.c``：ONU/PON 位域。
* SDK ``private/install_bsp/inc/blapi_system_bsp.h``：QDMA 掩码和环境字段。
* SDK ``linux/include/global_inc/uapi/ecnt_event_global/ecnt_event_serdes.h``、
  ``linux/drivers/char/arht_serdes_cfg.c``：SerDes 位域和接口定义。

刷写边界
--------

升级系统时只允许操作 ``ubi`` 分区，即 NAND 偏移
``0x00600000`` 起、长度 ``0x1b800000`` 的范围。升级流程应使用 MTD
分区名 ``ubi``，让 MTD/UBI 处理坏块，不应把整个 NAND 或 ``mtd0`` 当作
系统镜像目标。

bootloader 只允许在明确验证 FIP、签名、BL2/BL31 兼容性后替换
``0x00000000-0x00200000``。任何 bootloader 升级操作都必须保持
``uenv`` 的 ``0x00200000-0x00400000`` 和 ``dsd`` 的
``0x00400000-0x00600000`` 原样不变。``reserved_bmt`` 也不得写入。

U-Boot 配置
-----------

构建配置为 ``configs/xg2010g_defconfig``。它启用：

* AN7581 CPU、DDR、UART 和 SPI-NAND/SNFI；
* MTD、固定分区、UBI 和 UBIFS 命令；
* 将环境放在 ``spi-nand0`` 的绝对偏移 ``0x00200000``；
* 以 ``xg2010g`` 作为默认设备树。

由于 XG2010G 的板级 DTS 尚未进入 Linux 的 dts/upstream，本项目有意关闭
``CONFIG_OF_UPSTREAM``，而在 ``arch/arm/dts`` 维护很小的板级文件。
SoC 级 DTS 仍然引用 U-Boot 已同步的 Airoha ``en7581.dtsi``。

GitHub Actions 签名构建
-----------------------

仓库的 ``.github/workflows/build-mtd0.yml`` 用于在 GitHub Actions 上强制
构建 signed ``mtd0``；不再提供 ``skip``/未签名选项，也不会上传未签名
artifact：

* 编译 ``xg2010g_defconfig``，将 ``u-boot.bin`` 作为 BL33；
* 构建 TF-A 的 ``fiptool`` 和 ``cert_create``；
* 默认使用仓库内置的前导区、BL2 和 BL31，也可用 Secret/私有 URL 覆盖；
* 使用 ``cert_create`` 为 ``--tb-fw`` BL2、``--soc-fw`` BL31 和
  ``--nt-fw`` U-Boot/BL33 生成 Trusted Boot 证书链；
* 使用 ``fiptool create`` 生成 signed FIP；
* 将 FIP 写入 2 MiB mtd0 镜像中的 ``0x800`` 偏移，前导区保持原厂内容，
  其余尾部填充 ``0xff``。

默认仓库变量如下：

.. list-table::
   :header-rows: 1
   :widths: 30 20 50

   * - 变量
     - 默认值
     - 说明
   * - ``XG2010G_MTD0_SIZE``
     - ``0x200000``
     - mtd0/bootloader 固定 2 MiB，禁止越界到 uenv 或 dsd
   * - ``XG2010G_FIP_OFFSET``
     - ``0x800``
     - 原厂 mtd0 中 FIP 起始位置
   * - ``XG2010G_FIP_ALIGN``
     - ``0x400``
     - FIP payload 对齐，匹配已观察到的原厂 payload 偏移

可以先设置非敏感变量：

.. code-block:: console

   gh variable set XG2010G_MTD0_SIZE --repo naoki66/u-boot-xg2010g --body 0x200000
   gh variable set XG2010G_FIP_OFFSET --repo naoki66/u-boot-xg2010g --body 0x800
   gh variable set XG2010G_FIP_ALIGN --repo naoki66/u-boot-xg2010g --body 0x400

TF-A 工具版本固定在 workflow 内的 ``FIXED_TFA_REF=v2.13.0``，用于构建
``fiptool`` 和 ``cert_create``。手动运行 Actions 时不再提供 TF-A ref 输入，
也不再读取 ``XG2010G_TFA_REF`` 变量，避免误填不同 TF-A 版本导致 FIP/证书
产物不可复现。

使用仓库内置 BL2/BL31/prefix 时，只需要设置 Trusted Boot 私钥 Secret：

.. code-block:: console

   gh secret set XG2010G_TB_PRIVATE_KEY --repo naoki66/u-boot-xg2010g < private_key.pem

如需覆盖内置输入，可提供完整原厂 mtd0 的私有 URL，让 Actions 自动截取
``0x800`` 前导区并用 ``fiptool unpack`` 提取 BL2/BL31：

.. code-block:: console

   gh secret set XG2010G_ORIGINAL_MTD0_URL --repo naoki66/u-boot-xg2010g --body "https://private.example/original-mtd0.bin"
   gh variable set XG2010G_ORIGINAL_MTD0_SHA256 --repo naoki66/u-boot-xg2010g --body "<sha256>"

如果不能提供完整原厂 mtd0，也可以提供 ``XG2010G_BL2_B64``、
``XG2010G_BL31_B64``、``XG2010G_MTD0_PREFIX_B64``，或对应的 ``*_URL``。
由于 Secret 有大小限制，大二进制更适合放在私有对象存储或私有 Release
Asset，并用 SHA256 变量校验。

如果 Trusted Boot 私钥曾出现在聊天、日志、工单或其它非密钥管理系统中，
应视为已经泄露；继续制作可刷写 bootloader 前，应轮换为新私钥。

每次 Actions 构建都必须完成签名，否则 workflow 失败且不上传 artifact。
签名成功的 artifact 包含救砖和调试用文件：

.. list-table::
   :header-rows: 1
   :widths: 35 65

   * - 文件
     - 用途
   * - ``...-mtd0-signed.bin``
     - 完整 2 MiB ``/dev/mtd0`` bootloader 镜像，包含前导区、BL2、BL31、证书和 U-Boot/BL33
   * - ``...-fip-signed.bin``
     - 放入 mtd0 ``0x800`` 偏移的完整 signed FIP
   * - ``...-ubi-preloader.bin``
     - 包含 BL2 和 ``tb-fw-cert`` 的 signed FIP，用于 X 模式第一段 XMODEM
   * - ``...-ubi-bl31-uboot.fip``
     - BL31 + U-Boot/BL33 FIP，用于 X 模式第二段 XMODEM
   * - ``...-bl31.bin``
     - BL31 裸文件，便于核对和离线调试
   * - ``...-u-boot-raw.bin``
     - 本仓库本次编译出的 U-Boot/BL33 裸文件
   * - ``sha256sums.txt``
     - 所有产物的 SHA256 校验值

artifact 内也保留固定文件名的 ``ubi-preloader.bin``（BL2 + ``tb-fw-cert`` signed FIP）、
``ubi-bl31-uboot.fip``、``bl31.bin`` 和 ``u-boot-raw.bin``，方便救砖时
直接选择文件。

X 模式救砖
----------

如果 mtd0 写坏导致 NAND 不能正常启动，Airoha BootROM 通常仍可进入串口
X 模式加载临时引导。该流程需要 TTL 串口和支持 XMODEM 的终端工具。

首次使用救砖链时，可能需要先加载一次 ``bootext.ram``：

1. 断电。
2. 按住 ``RESET`` 键，同时通电启动。
3. 终端显示 ``CCCC`` 时表示设备已进入 X 模式。
4. 在终端菜单中选择“文件 -> 传输 -> XMODEM -> 发送”。
5. 选择 ``bootext.ram``，等待传输完成。之后通常不再需要重复加载该文件。

正式刷机步骤如下：

1. TTL 进入救砖模式：断电后按住 ``RESET`` 键，同时通电启动；按 ``X``/``x``，
   终端显示 ``CCCC`` 后进入 XMODEM 接收。
2. 传输 BL2/preloader：选择“文件 -> 传输 -> XMODEM -> 发送”，发送
   Actions 产物中的 ``...-ubi-preloader.bin`` 或 ``ubi-preloader.bin``。
   传输完成后设备会自动重启。
3. 再次进入 X 模式：重新断电，按住 ``RESET`` 后通电；提示
   ``Press x to load BL31 + U-Boot FIP`` 时输入 ``x``。
4. 传输 BL31 + U-Boot FIP：再次选择“文件 -> 传输 -> XMODEM -> 发送”，
   发送 ``...-ubi-bl31-uboot.fip`` 或 ``ubi-bl31-uboot.fip``。
5. 关键时序：第二段 XMODEM 进度到 100% 前提前按住 ``RESET``，等待设备灯
   完成闪烁并进入流水式闪烁后再松开。
6. 浏览器使用无痕模式访问 ``http://192.168.1.1/uboot.html``。
7. 选择 ``ubi-squashfs-sysupgrade.itb``；``BL2`` 选择
   ``...-ubi-preloader.bin``，``U-Boot`` 选择 ``...-ubi-bl31-uboot.fip``。
8. 系统固件上传会自动擦除并重建完整 UBI，等待数分钟完成刷写。
9. 刷写完成后务必断电重启设备。

U-Boot 启动后会对 RESET GPIO 做确认：默认需要持续按住 3 秒才进入 Web
Recovery，并在串口打印倒计时。中途松开会取消恢复并继续正常启动。可通过环境变量
调整等待时间：

.. code-block:: console

   setenv recovery_button_timeout 5
   saveenv

设置为 ``0`` 可恢复为检测到按键后立即进入恢复。

``bootext.ram`` 属于平台救援链文件，不由本仓库的 U-Boot 编译生成；需要
保留已验证可用的原厂/平台版本。

建议的验证顺序
--------------

在 Linux/WSL 环境中，先执行：

.. code-block:: console

   make CROSS_COMPILE=aarch64-linux-gnu- xg2010g_defconfig
   make CROSS_COMPILE=aarch64-linux-gnu- -j$(nproc)

若在 Windows 的 ``/mnt/d`` 这类 drvfs 挂载上构建，必须确认 Git 符号链接
和脚本可执行位被正确保留。更稳妥的做法是在 WSL 的 Linux 文件系统中构建，
或使用 ``git archive`` 导出一份临时构建副本后再编译。

构建得到的 ``u-boot.bin`` 是 BL33 候选文件。它还需要按照目标平台的
FIP 流程封装，不能直接当作完整 mtd0 写入。

使用平台提供的 ``fiptool`` 时，概念上的流程是：

.. code-block:: console

   fiptool info original.fip
   fiptool create --tb-fw bl2.bin --soc-fw bl31.bin --nt-fw u-boot.bin new.fip

上面的命令只表示 FIP 中 BL2、BL31、BL33 的关系。真实设备如果启用
Trusted Boot，通常还需要平台专用的证书、密钥、UUID、载入地址、签名参数
和可能的额外固件项；不能直接照抄此示例生成可启动镜像。

将 FIP 放入 mtd0 时，还要确认原厂镜像的前导数据、FIP 在 mtd0 中的偏移、
NAND 坏块处理和 BootROM 的复制规则。建议先通过串口验证 U-Boot 能识别
NAND 和分区，再在 Linux 中确认 ``cat /proc/mtd`` 的边界与本 DTS 一致。
